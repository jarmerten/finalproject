@model IEnumerable<GrindStone.Models.JobProducts>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Jobs.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Products.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Quanity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Comments)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Jobs.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Products.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Quanity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Comments)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.JobProductID }) |
            @Html.ActionLink("Details", "Details", new { id=item.JobProductID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.JobProductID })
        </td>
    </tr>
}

</table>
